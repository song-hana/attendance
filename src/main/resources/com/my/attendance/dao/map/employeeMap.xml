<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org/DTD Mapper 3.0//EN'
	'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace='com.my.attendance.dao.map.EmployeeMap'>
	<select id='selectEmployee' resultType='employee'>
		select *
		from employees
		where emp_id = '${empId}'
		and emp_pw = '${empPw}'
	</select>
	
	<select id='selectEmployeeId' resultType='String'>
		select emp_id
		from employees
		where emp_name = '${empName}'
		and emp_ph = '${empPh}'
	</select>
	
	<select id='selectEmployeePw' resultType='String'>
		select emp_pw
		from employees
		where emp_id = '${emId}'
		and emp_pino = '${empPino}'
	</select>
	
	<insert id='insertEmployee'>
		insert into attendance.employees(employee_no, emp_id, emp_pw, emp_name, emp_addr,
		emp_detailaddr, emp_postcode, emp_ph, emp_email, hire_date, emp_position,
		emp_pino, profile_name, company_id)
		values(attendance.emp_empno_seq.nextval, #{empId}, #{empPw}, #{empName}, #{empAddr},
		#{empDetailAddr}, #{empPostcode}, #{empPh}, #{empEmail}, #{hireDate},
		#{empPosition}, #{empPino}, #{profileName}, #{companyId})
	</insert>
	
	<select id='checkEmployee' resultType='Int'>
		select count(*)
		from employees
		where emp_id = #{empId}
	</select>
</mapper>
